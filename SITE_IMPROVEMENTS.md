# Site Improvement Plan

Working branch: `dev`.

## Completed

- [x] Fixed duplicated hero `h1` text in the DOM so SEO and screen readers see one clear primary heading.
- [x] Synced FAQ JSON-LD with the five visible FAQ questions on the page.
- [x] Added lazy-loading and async decoding to below-the-fold image assets.

## Recommended Next

- [ ] Replace temporary photo placeholders with real HM-PRO job photos, especially blower door, thermal camera, report, and before/after attic images.
- [ ] Add a second hero CTA for direct calling: `Call (206) 530-0018`.
- [ ] Add a mobile sticky CTA bar with `Call` and `Book`.
- [ ] Add a short post-pricing CTA: `Not sure which package fits? Start with a free phone consultation.`
- [ ] Add `WebPage` and `BreadcrumbList` schema.
- [ ] Expand `LocalBusiness` schema with confirmed `sameAs`, `openingHours`, `geo`, and real review/rating data only after verification.
- [ ] Convert final production photos to WebP/AVIF and add explicit width/height values for layout stability.
- [ ] Verify the final deployed `og:image` URL after publishing.
- [ ] Add real review snippets or confirmed Google/Thumbtack counts once available.
